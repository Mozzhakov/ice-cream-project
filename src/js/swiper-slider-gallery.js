const swiper = new Swiper('.swiper-gallery', {
  effect: 'fade',
  // loop: true,
  // loopedSlides: 8,
  autoplay: {
    delay: 1000,
    stopOnLastSlide: false,
    disableOnInteraction: false
  }
});
